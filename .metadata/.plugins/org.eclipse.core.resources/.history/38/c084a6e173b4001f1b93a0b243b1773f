<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <style>
        /* General body styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Cleaner, modern font */
            background-color: #e9f1f7; /* Soft light blue background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Main title styling */
        h1 {
            text-align: center;
            color: #333;
            font-size: 2em;
            margin-bottom: 20px;
        }

        /* Form container styling */
        form {
            background-color: #fff;
            border-radius: 8px;
            padding: 30px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px; /* Added maximum width for responsiveness */
            text-align: left; /* Align text to the left for better readability */
        }

        /* Label styling */
        label {
            font-weight: bold;
            color: #444;
            margin-bottom: 10px;
            display: block;
            font-size: 1.1em;
        }

        /* Input fields styling */
        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 1em;
            background-color: #f7f9fc; /* Light grayish background for input fields */
        }

        /* Focus effect on input fields */
        input[type="text"]:focus,
        input[type="password"]:focus {
            border-color: #007BFF; /* Blue border on focus */
            outline: none;
        }

        /* Submit button styling */
        input[type="submit"] {
            width: 100%;
            padding: 12px;
            background-color: #007BFF; /* Blue button */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1.1em;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        /* Hover effect on submit button */
        input[type="submit"]:hover {
            background-color: #0056b3; /* Darker blue on hover */
            transform: translateY(-2px); /* Slight upward movement */
        }

        /* Responsive Design: Adjust for smaller screens */
        @media (max-width: 600px) {
            h1 {
                font-size: 1.5em;
            }
            form {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <form method="post" action="checkadminlogin">
        <h1>Admin Login</h1>
        <label>Enter your username:</label>
        <input type="text" name="sid" required><br/>
        <label>Enter your Password:</label><br/
        <input type="password" name="spwd" required><br/>
        <input type="submit" value="Login">
    </form>
</body>
</html>
